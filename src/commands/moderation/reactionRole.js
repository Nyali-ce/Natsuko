export default {
    name: 'reactionrole',
    template: 'heheheha',
    description: 'heheheha',
    run: async (message, args, client) => {
        // reactionRole message emoji:roleID
        const guild_ID = message.guild.id;
    }
}